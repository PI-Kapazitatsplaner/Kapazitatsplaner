<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="header.css">
    <link rel="stylesheet" href="common.css">
    <script src="controller.js"></script>
    <title>Document</title>
</head>
<body id="body" class="content">
    <%- header %>
    <div class="mainContent">
        <h1 id="text" class="text id">Team Kalender</h1>
    </div>
</body>
<script>
    /*
    setColors("light");
    setButtonColors("light");
    function setButtonColors(val){
        var button = document.getElementsByClassName("id");
        if (val == "dark") {
            for (let i = 0; i < button.length; i++) {
                button.item(i).classList.add("button-dark");
                button.item(i).classList.remove("button");
            }            
        }
        else if (val == "light") {
            for (let i = 0; i < button.length; i++) {
                button.item(i).classList.add("button");
                button.item(i).classList.remove("button-dark");
            } 
        }
    }
    */
</script>
</html>