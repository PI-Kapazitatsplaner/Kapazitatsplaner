generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_CONNECTION")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  
  name  String?
}
